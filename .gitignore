# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Secrets — Phase 0 requires env injection, never files (Playbook Phase 0)
.env
.env.*
!.env.example

# Terraform — never commit state; it holds resource metadata and can hold secrets
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# OS
.DS_Store

# mutmut working copy (ADR-023)
mutants/
.mutmut-cache
